apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - issuers.yaml
patches:     
  - target:
      group: cert-manager.io
      version: v1
      kind: ClusterIssuer
      name: letsencrypt-staging
      namespace: istio-system
    patch: |-
      - op: add
        path: /spec/acme/solvers/-
        value:
          selector: {}
          dns01:
            cloudDNS:
              project: phx-01h28kjpmp42v8xh59v0qvfpxa
              hostedZoneName: dhis2-managed-dns-zone
      - op: add
        path: /spec/acme/email
        value: simardeep.singh@phac-aspc.gc.ca

  - target:
      group: cert-manager.io
      version: v1
      kind: ClusterIssuer
      name: letsencrypt-production
      namespace: istio-system
    patch: |-
      - op: add
        path: /spec/acme/solvers/-
        value:
          selector: {}
          dns01:
            cloudDNS:
              project: phx-01h28kjpmp42v8xh59v0qvfpxa
              hostedZoneName: dhis2-managed-dns-zone
      - op: add
        path: /spec/acme/email
        value: simardeep.singh@phac-aspc.gc.ca